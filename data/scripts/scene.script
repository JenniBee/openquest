#include "scripts\base.inc"

////////////////////////////////////////////////////////////////////////////////
on "LeftClick"
{
var ZobMouseX = Scene.MouseX + 12;
var ZobMouseY = Scene.MouseY + 45;

  // when the scene is left-clicked, just send the actor to the specified point
  actor.GoTo(ZobMouseX, ZobMouseY);
}
